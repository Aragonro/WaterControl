@model WoterCantrol.Models.UserViewModel

<nav class="navbar navbar-inverse">
    <div class="container-fluid" id="navdiv">
        <div class="navbar-header">
            <a class="navbar-brand" href="/" style="font-family: 'Times New Roman', Times, serif; font-style: italic; ">WaterControl</a>
        </div>
        <div>
            <ul class="nav navbar-nav">
                <li>@Html.ActionLink("Home", "Index", "Home")</li>
                @if (Model.Role == "User")
                {
                    <li>@Html.ActionLink("My Monitorings", "Monitorings", "Home")</li>
                    <li>@Html.ActionLink("My Sensors", "Sensors", "Sensor")</li>
                }
            </ul>
            <ul class="nav navbar-nav navbar-right">
                @if (Model.Role == "Anonym")
                {
                    <li>@Html.ActionLink("Sign In", "SignIn", "Home")</li>
                    <li>@Html.ActionLink("Sign Up", "SignUp", "Home")</li>
                }
                @if (Model.Role == "User")
                {
                    <li ><a onclick="deleteAllCookie();document.location.href = '/'">Log out</a></li>
                }
            </ul>
        </div>
    </div>
</nav>
